import 'package:flutter/material.dart';

class ToastMessageView extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Show all toast messages")
    );  
  }
}