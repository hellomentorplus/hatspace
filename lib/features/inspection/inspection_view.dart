import 'package:flutter/material.dart';

class InspectionView extends StatelessWidget {
  const InspectionView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Center(
        child: Text('Inspection View'),
      );
}
